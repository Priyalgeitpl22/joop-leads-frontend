export enum ThreadType {
    UNASSIGNED = "unassigned",
    ASSIGNED = "assigned",
    OPEN = "open",
    CHAT = "chat",
    BOTS = "bots",
}

export enum CampaignStatus {
    DRAFT = "draft",
    PASSED = "passed",
    RUNNING = "running",
    SCHEDULED = "scheduled",
    COMPLETED = "completed"
}