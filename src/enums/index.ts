export enum ThreadType {
    UNASSIGNED = "unassigned",
    ASSIGNED = "assigned",
    OPEN = "open",
    CHAT = "chat",
    BOTS = "bots",
}
  
export enum CampaignStatus {
    DRAFT = 'DRAFT',
    SCHEDULED = 'SCHEDULED',
    ACTIVE = 'ACTIVE',
    COMPLETED = 'COMPLETED',
    PAUSED = 'PAUSED'
}