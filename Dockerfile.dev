# Development: run Vite dev server with hot reload
FROM node:20-alpine

WORKDIR /app

COPY package*.json ./
RUN npm install

# Source is mounted at runtime via docker-compose
COPY . .

EXPOSE 5173

CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
